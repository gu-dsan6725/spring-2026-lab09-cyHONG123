[!](mcp.png)

So the agent tried to use tool to connect mcp server, but it fails for unknown reasons. I guess it is because the old api broke and old mcp link is also problematic (i can not open this mcp link!!). Braintrust simply call the mcp tools and spend lots of tokens (more than default tool!!). It is clear here duckdb tool is better since mcp tool broke. 